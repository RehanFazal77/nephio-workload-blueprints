# Interface for F1u (User Plane - CUUP to DU)
apiVersion: req.nephio.org/v1alpha1
kind: Interface
metadata:
  name: f1u
  annotations:
    nephio.org/network-name: f1
    config.kubernetes.io/local-config: "true"
    specializer.nephio.org/owner: workload.nephio.org/v1alpha1.NFDeployment.cuup
    specializer.nephio.org/namespace: oai-ran
spec:
  networkInstance:
    name: vpc-cudu-f1
  cniType: macvlan
