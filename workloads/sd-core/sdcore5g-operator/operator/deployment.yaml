apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: sdcore5g
  name: sdcore5g-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: sdcore5g-operator
      app.kubernetes.io/component: controller
  template:
    metadata:
      name: sdcore5g-operator
      labels:
        app.kubernetes.io/name: sdcore5g-operator
        app.kubernetes.io/component: controller
    spec:
      serviceAccountName: sdcore5g-operator
      containers:
      - name: kube-rbac-proxy
        image: gcr.io/kubebuilder/kube-rbac-proxy:v0.8.0
        args:
        - --secure-listen-address=0.0.0.0:8443
        - --upstream=http://127.0.0.1:8080/
        - --logtostderr=true
        - --v=10
        ports:
        - containerPort: 8443
          name: https
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 5m
            memory: 64Mi
      - name: operator
        image: sdcore5g-operator:v1.0.0
        imagePullPolicy: Never
        command:
        - /sdcore5g-operator
        args:
        - --health-probe-bind-address=:8081
        - --metrics-bind-address=127.0.0.1:8080
        - --leader-elect
        env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.namespace
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
